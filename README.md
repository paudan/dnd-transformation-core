## DnD transformations core API

M2M Transformations Core API, which implements all the necessary abstract M2M transformation processing and could be used to develop partial M2M transformation solutions, using UML profile based user-specified specifications, for compatible CASE tools.

This framework uses [JSON-Java](https://github.com/stleary/JSON-java) library by Sean Leary for serialization and textual representation in JSON.

